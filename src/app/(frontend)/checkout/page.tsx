"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { ArrowRight, Check, CreditCard, Loader2, MapPin, ShieldCheck, Truck } from "lucide-react";
import { type FormEvent, useState } from "react";

import { Button } from "@/components/ui/button";
import { CartLine, useCart } from "@/features/cart/cart-context";

const priceFormatter = new Intl.NumberFormat("fa-IR");

export default function CheckoutPage() {
  const router = useRouter();
  const { items, subtotal, clear } = useCart();
  const [submitted, setSubmitted] = useState(false);
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState("");
  const submit = async (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    setSubmitting(true);
    setSubmitError("");
    const form = new FormData(event.currentTarget);
    const response = await fetch("/api/orders", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ contact: Object.fromEntries(form) }) });
    if (response.status === 401) { router.push("/login?next=/checkout"); return; }
    if (!response.ok) {
      const result = await response.json().catch(() => null) as { error?: string } | null;
      setSubmitting(false);
      setSubmitError(result?.error ?? "ثبت سفارش انجام نشد. لطفاً دوباره تلاش کنید.");
      return;
    }
    setSubmitted(true);
    clear();
  };

  if (submitted) return <main className="min-h-[60vh]"><div className="container-shell py-20 text-center"><div className="mx-auto grid size-16 place-items-center rounded-full bg-wine text-white"><Check className="size-8" /></div><h1 className="mt-7 text-4xl font-medium">درخواست شما ثبت شد</h1><p className="mx-auto mt-4 max-w-lg text-sm leading-7 text-muted-foreground">مشاور NPGroup برای تأیید جزئیات، زمان تحویل و روش پرداخت با شما تماس می‌گیرد.</p><Button asChild className="mt-8 h-12 rounded-none bg-wine px-8 hover:bg-ink"><Link href="/shop">بازگشت به فروشگاه</Link></Button></div></main>;

  return <main className="min-h-[60vh]"><div className="container-shell py-8 sm:py-14"><div className="mb-10 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold tracking-[0.18em] text-wine">پرداخت</p><h1 className="mt-3 text-4xl font-medium sm:text-5xl">تکمیل سفارش</h1></div><Link href="/cart" className="hidden items-center gap-2 text-xs text-muted-foreground hover:text-wine sm:flex"><ArrowRight className="size-4" />بازگشت به سبد</Link></div>{items.length ? <form onSubmit={submit} className="grid gap-10 lg:grid-cols-[minmax(0,1fr)_22rem] lg:items-start"><div className="min-w-0 space-y-8"><section className="border border-black/10 p-6 sm:p-8"><div className="flex items-center gap-3"><MapPin className="size-5 text-wine" /><div><h2 className="text-xl font-medium">اطلاعات تماس و ارسال</h2><p className="mt-1 text-xs text-muted-foreground">برای پیگیری سفارش و هماهنگی تحویل</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm">نام و نام خانوادگی<input required name="name" autoComplete="name" className="mt-2 h-11 w-full border border-black/15 bg-white px-3 text-sm outline-none focus:border-wine" /></label><label className="text-sm">شماره موبایل<input required name="phone" type="tel" inputMode="tel" autoComplete="tel" className="mt-2 h-11 w-full border border-black/15 bg-white px-3 text-sm outline-none focus:border-wine" /></label><label className="text-sm">استان<select required name="province" className="mt-2 h-11 w-full border border-black/15 bg-white px-3 text-sm outline-none focus:border-wine"><option value="">انتخاب استان</option><option>تهران</option><option>البرز</option><option>اصفهان</option><option>فارس</option></select></label><label className="text-sm">شهر<input required name="city" autoComplete="address-level2" className="mt-2 h-11 w-full border border-black/15 bg-white px-3 text-sm outline-none focus:border-wine" /></label><label className="text-sm sm:col-span-2">نشانی کامل<input required name="address" autoComplete="street-address" className="mt-2 h-11 w-full border border-black/15 bg-white px-3 text-sm outline-none focus:border-wine" /></label><label className="text-sm">کد پستی<input required name="postal" inputMode="numeric" autoComplete="postal-code" className="mt-2 h-11 w-full border border-black/15 bg-white px-3 text-sm outline-none focus:border-wine" /></label></div></section><section className="border border-black/10 p-6 sm:p-8"><div className="flex items-center gap-3"><Truck className="size-5 text-wine" /><div><h2 className="text-xl font-medium">روش تحویل</h2><p className="mt-1 text-xs text-muted-foreground">هزینه و زمان دقیق پس از بررسی شهر تأیید می‌شود</p></div></div><label className="mt-6 flex cursor-pointer gap-3 border border-wine bg-wine/5 p-4"><input type="radio" name="delivery" value="advisor" defaultChecked className="accent-[var(--wine)]" /><span><span className="block text-sm font-medium">هماهنگی تحویل و نصب توسط NPGroup</span><span className="mt-1 block text-xs text-muted-foreground">پیشنهاد مناسب برای مبلمان و سفارش‌های سفارشی</span></span></label></section><section className="border border-black/10 p-6 sm:p-8"><div className="flex items-center gap-3"><CreditCard className="size-5 text-wine" /><div><h2 className="text-xl font-medium">روش پرداخت</h2><p className="mt-1 text-xs text-muted-foreground">روش نهایی پس از تماس مشاور با شما قطعی می‌شود</p></div></div><label className="mt-6 flex cursor-pointer gap-3 border border-black/10 p-4"><input type="radio" name="payment" value="gateway" defaultChecked className="accent-[var(--wine)]" /><span><span className="block text-sm font-medium">درگاه پرداخت آنلاین</span><span className="mt-1 block text-xs text-muted-foreground">برای محصولات آماده ارسال یا بیعانه سفارش سفارشی</span></span></label><label className="mt-3 flex cursor-pointer gap-3 border border-black/10 p-4"><input type="radio" name="payment" value="invoice" className="accent-[var(--wine)]" /><span><span className="block text-sm font-medium">فاکتور و پرداخت مرحله‌ای</span><span className="mt-1 block text-xs text-muted-foreground">مناسب سفارش‌های با مبلغ بالا، با هماهنگی مشاور</span></span></label></section></div><aside className="min-w-0 overflow-hidden border border-black/10 bg-secondary/35 p-6 lg:sticky lg:top-32"><h2 className="text-lg font-medium">مرور سفارش</h2><div className="mt-4 max-h-72 space-y-1 overflow-x-hidden overflow-y-auto border-b border-black/10 pb-4 pe-1">{items.map((item) => <CartLine key={item.key} item={item} compact />)}</div><div className="space-y-3 py-5 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">جمع کالاها</span><span>{priceFormatter.format(subtotal)} تومان</span></div><div className="flex justify-between"><span className="text-muted-foreground">ارسال</span><span>پس از هماهنگی</span></div><div className="flex justify-between border-t border-black/10 pt-4 text-base font-semibold"><span>مبلغ سفارش</span><span className="text-wine">{priceFormatter.format(subtotal)} تومان</span></div></div>{submitError ? <p role="alert" className="mb-3 text-xs leading-6 text-destructive">{submitError}</p> : null}<Button type="submit" disabled={submitting} className="h-12 w-full rounded-none bg-wine hover:bg-ink">{submitting ? <Loader2 className="animate-spin" /> : <ShieldCheck />}{submitting ? "در حال ثبت سفارش" : "ثبت درخواست سفارش"}</Button><p className="mt-4 text-center text-[0.68rem] leading-5 text-muted-foreground">با ثبت این فرم، جزئیات سفارش برای بررسی و هماهنگی ارسال به NPGroup منتقل می‌شود.</p></aside></form> : <div className="border border-dashed border-black/15 py-20 text-center"><h2 className="text-2xl font-medium">سبد خرید خالی است</h2><Button asChild className="mt-7 rounded-none bg-wine hover:bg-ink"><Link href="/shop">بازگشت به فروشگاه</Link></Button></div>}</div></main>;
}
